{% assign s = block.settings %}

{% comment %} --- Wrapper Classes --- {% endcomment %}
{% assign classes = 'absolute w-full z-floating flex flex-col items-center gap-2 cursor-pointer transition-opacity hover:opacity-80' %}

{% if s.surface_override == false and s.surface_override == true %}
  {% assign classes = classes | push: "surface-{{ s.surface_id | default: 'default' }}" %}
{% endif %}

{% assign block_classes = classes | join: ' ' %}

{% comment %} --- Styles Array Building --- {% endcomment %}
{% assign styles = blank %}

{% unless s.surface_override == false %}
  {% assign styles = styles | push_if: '--text: {{ s.text_color }}', s.text_color_override %}
{% endunless %}

{% assign styles = styles | push: 'bottom: {{ s.bottom_offset }}rem' %}

{% assign block_styles = styles | join: ';' %}

{% comment %} --- Component Render --- {% endcomment %}
<div
  class="{{ block_classes }}"
  style="{{ block_styles }}"
  onclick="window.scrollTo({ top: window.innerHeight, behavior: 'smooth' });"
  {{ context.attached_attributes }}
>
  {% if s.label != blank %}
    {% render 'typography', text: s.label, tag: 'span', display_size: 'caption', weight: 'bold', class: 'uppercase' %}
  {% endif %}

  <div class="animate-bounce">
    {% render 'icon', name: 'chevron-down', class: 'w-5 h-5 text-body' %}
  </div>
</div>

{% schema %}
{
  "name": "Scroll Indicator",
  "settings": [
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "bottom_offset",
      "label": "Bottom Offset",
      "info": "Lift up to clear marquees.",
      "min": 0,
      "max": 8,
      "step": 0.25,
      "default": 2,
      "unit": "rem"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "label",
      "label": "Label",
      "default": "Scroll"
    },
    {
      "type": "header",
      "content": "Style"
    },
        {
      "type": "header",
      "content": "Overrides",
      "conditional": "true"
    },
    {
      "type": "checkbox",
      "id": "surface_override",
      "label": "Override Color Scheme",
      "default": false,
      "conditional": "true"
    },
    {
      "type": "surface",
      "id": "surface_id",
      "label": "Color Scheme",
      "default": "default",
      "conditional": "setting.surface_override == true"
    },
    {
      "type": "checkbox",
      "id": "text_color_override",
      "label": "Override Text Color",
      "default": false,
      "conditional": "true"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Custom Color",
      "default": "#ffffff",
      "conditional": "true and setting.text_color_override"
    }
  ]
}
{% endschema %}
