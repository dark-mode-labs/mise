<div
  {{ context.attached_attributes }}
>
  <div style="max-width: {{ block.settings.logo_width }}rem">
    {% render 'media', src: block.settings.image, fit: 'contain' %}
  </div>
</div>

{% schema %}
{
  "name": "Logo Icon",
  "settings": [
    { "type": "image_picker", "id": "image", "label": "Image" },
    { "type": "range", "id": "logo_width", "label": "Width", "min": 3, "max": 20, "step": 0.5, "default": 9, "unit": "rem" }
  ]
}
{% endschema %}
